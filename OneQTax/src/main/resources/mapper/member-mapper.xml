<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTO Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.spring.oneqtax.member.repository.MemberMapper">

    <!-- 로그인 -->
<!--    <select id="loginMember" parameterType="HashMap" resultType="com.spring.oneqtax.member.domain.MemberVO">-->
<!--        SELECT *-->
<!--        FROM member-->
<!--        WHERE email = #{email}-->
<!--          AND password = #{password}-->
<!--    </select>-->

    <select id="loginMember" resultType="com.spring.oneqtax.member.domain.MemberVO">
        select * from member
        where email=#{email} and password=#{password}
    </select>

    <!-- 우리집 돈관리  -->
    <select id="getMemberByEmail" resultType="com.spring.oneqtax.member.domain.MemberVO">
        select * from member
        where email=#{spouseEmail}
    </select>

    <!-- 회원 전체 조회 -->
    <select id="getAllMember" resultType="com.spring.oneqtax.member.domain.MemberVO">
        SELECT *
        FROM member
    </select>


<!--    <select id="selectNameOfMember" parameterType="String" resultType="com.spring.oneqtax.member.domain.MemberVO">-->
<!--        SELECT *-->
<!--        FROM member-->
<!--        where EMAIL = #{email}-->
<!--    </select>-->


<!--    <insert id="insertMember" parameterType="com.spring.oneqtax.member.model.dto.Member">-->
<!--        INSERT INTO MEMBER (MEMBER_IDEMAIL, NAME, PASSWORD, PHONE,gender,ADDRESS,IDENTITYNUM)-->
<!--        VALUES (#{email}, #{name},#{password}, #{phone},#{gender},#{address},#{identityNum})-->
<!--    </insert>-->

<!--    <update id="updateMember" parameterType="com.spring.oneqtax.member.model.dto.Member">-->
<!--        UPDATE member-->
<!--        SET password = #{password}, name = #{name}, phone = #{phone}-->
<!--        WHERE EMAIL = #{email}-->
<!--    </update>-->

<!--    <delete id="deleteMember" parameterType="String">-->
<!--        DELETE FROM member WHERE EMAIL = #{email}-->
<!--    </delete>-->




</mapper>